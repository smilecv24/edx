<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>EDX</title>
</head>
<body>
<ul>
<% animals.forEach( (animal) => { %>
   <li>
    <%= animal.name %>, <%= animal.species %>, <%= animal.breed %>,
    <%= animal.gender %>, <%= animal.traits %>, <%= animal.age %>
   </li>
<% }); %>
</ul>
</body>
</html>